# Núcleo del harness (loop + autoaprendizaje)
anthropic>=0.40

# El motor local (LM Studio / Ollama / vLLM) se habla por HTTP con la stdlib:
# tools/backends.py no añade dependencias para eso.

# Opcionales por skill/subagente (descomenta lo que uses)
# biopython>=1.83        # skill pubmed_search (Entrez) + contraste de paper_review
# pypdf>=4.0             # skill paper_review (lectura de PDFs)
# python-docx>=1.1        # skill build_docx (subagente docs) + paper_review (lectura de .docx)
# python-pptx>=0.6        # skill build_pptx (subagente docs)
# numpy>=1.26            # subagente signals
# scipy>=1.12            # subagente signals
# matplotlib>=3.8        # figuras de signals

# Desarrollo
# ruff>=0.4
# black>=24.0
# pytest>=8.0
