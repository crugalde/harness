# Secrets / local env (kept outside the repo, e.g. ~/.config/harness/.env)
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Office temp/lock files
~$*

# Observabilidad y estado local (se regeneran)
shared/traces/*.jsonl
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
